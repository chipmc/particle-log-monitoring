{
  "name": "particle-fleet-operations-scripts",
  "version": "1.0.0",
  "description": "CLI tools for local inspection of particle-fleet-operations data (plain text output)",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "timeline": "npm run build && node dist/timeline.js"
  },
  "dependencies": {
    "@aws-sdk/client-cloudformation": "^3.621.0",
    "@aws-sdk/client-dynamodb": "^3.621.0",
    "@aws-sdk/client-s3": "^3.621.0",
    "@aws-sdk/lib-dynamodb": "^3.621.0",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.0",
    "typescript": "^5.5.4"
  }
}
